# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: Java CI with Maven

on:
  workflow_dispatch:
  push:
    branches: [ "make-vertx-redisques-build-pass-on-github-actions" ]
  pull_request:
    branches: [ "develop" ]

jobs:
  build_maven:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v3
      - name: Set up JDK 11
        uses: actions/setup-java@v3
        with: # running setup-java again overwrites the settings.xml
          distribution: 'adopt'
          java-version: '11'
          server-id: 'sonatype-nexus-staging'
          server-username: CI_DEPLOY_USERNAME
          server-password: CI_DEPLOY_PASSWORD

      - name: Start Redis
        uses: supercharge/redis-github-action@1.4.0
        with:
          redis-version: 4

      - name: Publish to the Maven Central Repository
        run: mvn --batch-mode deploy
        env:
          CI_DEPLOY_USERNAME: ${{ secrets.CI_DEPLOY_USERNAME }}
          CI_DEPLOY_PASSWORD: ${{ secrets.CI_DEPLOY_PASSWORD }}

      - name: Set up Java for publishing to GitHub Packages
        uses: actions/setup-java@v3
        with:
          java-version: '11'
          distribution: 'adopt'
      - name: Publish to GitHub Packages
        run: mvn --batch-mode deploy
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}

#      - name: Publish to GitHub Packages Apache Maven
#        run: mvn deploy
#        env:
#          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }} # GITHUB_TOKEN is the default env for the password

#      - name: Set up Apache Maven Central
#        uses: actions/setup-java@v3
#        with: # running setup-java again overwrites the settings.xml
#          distribution: 'temurin'
#          java-version: '11'
#          server-id: 'sonatype-nexus-staging'
#          server-username: CI_DEPLOY_USERNAME
#          server-password: CI_DEPLOY_PASSWORD
#          gpg-private-key: ${{ secrets.CI_PGP_PRIVATE_KEY }}
#          gpg-passphrase: CI_PGP_PASSWORD
#
#      - name: Publish to Apache Maven Central
#        run: mvn deploy
#        env:
#          CI_DEPLOY_USERNAME: ${{ secrets.CI_DEPLOY_USERNAME }}
#          CI_DEPLOY_PASSWORD: ${{ secrets.CI_DEPLOY_PASSWORD }}
#          CI_PGP_PASSWORD: ${{ secrets.CI_PGP_PASSWORD }}